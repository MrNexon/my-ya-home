version: "3.4"

services:
  app:
    image: "registry.sostoya.com/smart_home:0.0.1"
    expose:
      - 3000
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.registry.rule=Host(`iot.mtdl.ru`)"
      - "traefik.http.routers.registry.entrypoints=web"
networks:
  proxy:
    external: true
