version: "3.4"

services:
  app:
    build: .
    expose:
      - 3000
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.registry.rule=Host(`iot.mtdl.ru`)"
      - "traefik.http.routers.registry.entrypoints=web"
networks:
  proxy:
    external: true
