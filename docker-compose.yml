version: "3.4"

services:
  app:
    image: "registry.sostoya.com/smart_home:0.0.3"
    expose:
      - 3000
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.smarthome.rule=Host(`iot.mtdl.ru`)"
      - "traefik.http.routers.smarthome.entrypoints=secure"
      - "traefik.http.routers.smarthome.tls.certresolver=ssl"
  broker:
    image: eclipse-mosquitto
    expose:
      - 1883
    networks:
      - proxy
    volumes:
      - smarthome_broker/mqtt.conf:/mosquitto/config/mosquitto.conf
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.smarthome_broker.rule=Host(`iot.mtdl.ru`) && PathPrefix(`/events`)"
      - "traefik.http.routers.smarthome_broker.entrypoints=web"
networks:
  proxy:
    external: true

volumes:
  smarthome_broker:
